{
  "name": "tiger-calendar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp sw.js dist/sw.js && cp -r icons dist/icons && cp manifest.json dist/manifest.json && mv dist/dev.html dist/index.html",
    "preview": "vite preview"
  },
  "dependencies": {
    "@rollup/rollup-darwin-arm64": "^4.60.0",
    "firebase": "^10.12.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "vite-plugin-singlefile": "^2.3.2"
  }
}
